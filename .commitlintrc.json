{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["feat", "fix", "docs", "chore", "perf", "refactor", "test", "style", "build", "ci"]
    ],
    "scope-enum": [2, "always", ["amazon-shopping", "release", "deps"]],
    "subject-case": [0]
  }
}
